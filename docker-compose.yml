services:
  iptv-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: xtremflow
    restart: unless-stopped
    networks:
      - nginx_default

networks:
  nginx_default:
    external: true
